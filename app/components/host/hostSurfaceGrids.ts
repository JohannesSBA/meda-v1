/** Dense KPI grids (analytics overview, payout summary blocks). */
export const HOST_METRIC_GRID_DENSE = "grid gap-4 md:grid-cols-2 xl:grid-cols-4";

/** Three-up metric row (e.g. subscription tab). */
export const HOST_METRIC_GRID_THREE = "grid gap-4 md:grid-cols-3";

/** Two-column metric pair (e.g. customer detail). */
export const HOST_METRIC_PAIR_GRID = "grid gap-3 sm:grid-cols-2";

/** Three metric strip inside host page section cards. */
export const HOST_SECTION_STRIP_GRID = "grid gap-3 md:grid-cols-3";

/** Overview “today” task cards. */
export const HOST_OVERVIEW_TASK_GRID = "grid gap-3 sm:grid-cols-2 xl:grid-cols-4";
