/**
 * Auth client -- Neon Auth client for client-side session and auth actions.
 */

'use client';

import { createAuthClient } from '@neondatabase/auth/next';
export const authClient = createAuthClient();